name: diag-fix12-verify

# 一時的な調査用ワークフロー（v1.29修正1・2の効果を2026-08-21・
# 2026-08-22の実データで検証）。本番パイプライン（daily.yml・
# set_headline.yml・post_draft.yml）には一切影響しない。
# 調査完了後、本ファイルとscripts/_diag_fix12_verify.pyは削除する。

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diag:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 依存関係
        run: pip install -q -r requirements.txt

      - name: 修正1・2の効果を8/21・8/22の実データで検証
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python3 scripts/_diag_fix12_verify.py
